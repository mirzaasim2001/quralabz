/**
 * Subtle cinematic film-grain texture, layered above the aurora and
 * below all interactive content. See `.noise-overlay` in globals.css.
 */
export default function GrainOverlay() {
  return <div className="noise-overlay" aria-hidden="true" />;
}
